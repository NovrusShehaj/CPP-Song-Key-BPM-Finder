# Abstract runtime dependencies. CI installs the pinned lockfile.
# soundfile is the explicit decode/probe backend used by librosa.load and sf.info.
librosa>=0.10.0,<0.12
numpy>=1.24,<3
soundfile>=0.12.1,<0.14
